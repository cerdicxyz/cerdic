# Fill via `pnpm run keygen` output, plus deployment addresses from
# tiny/contracts/script/Deploy.s.sol

RPC_URL=http://127.0.0.1:8545
VAULT_ADDRESS=0x...

TEE_ADDRESS=0x...
TEE_PRIVATE_KEY=0x...

BOX_PUBLIC_KEY_B64=...
BOX_SECRET_KEY_B64=...

PORT=8787
# 6-decimal mock mark price used to compute settlement on /close
MOCK_MARK_PRICE=65000000000
